# Core dependencies that work with Python 3.9
# Note: fastmcp requires Python 3.10+, install separately if needed
py-clob-client
ib_insync
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv
requests
aiohttp
websockets
numpy
pandas
pytest>=7.0
